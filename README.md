# Baldur's Gate 3 Scripts

This script automatically create a backup of the given save directory. You need to edit two variables in each script:
- `SAVE_DIRECTORY`: the path to your honor mode save directory,
- `BACKUP_DIRECTORY`: the path to the directory where you want to store the backups.

Considering triggering these scripts through a shortcut key for a better experience.
